name: doit_app
description: DOIT Rajasthan - Startup Proposal Management App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.1
  go_router: ^12.1.3
  google_fonts: ^6.1.0
  intl: ^0.18.1
  file_picker: ^6.1.1
  shimmer: ^3.0.0
  cached_network_image: ^3.3.1
  flutter_animate: ^4.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
